export interface email {
    email: string,
    id: string
}

export interface user {
    name: string,
    email: string,
    phoneNo: string,
    password: string,
    confirm: boolean,
    id: string
}